<nav class="navbar navbar-expand-md bg-dark rounded-bottom-2 mb-3" data-bs-theme="dark">
	<div class="container-fluid">
		<a href="{{path('app_main')}}"><img id="logo" src="{{asset('assets/images/motus_logo.png')}}" alt="Logo Motus"></a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
						Menu
					</a>
					<ul class="dropdown-menu">
						<li>
							<a class="dropdown-item" href="{{path('app_main')}}">Jouer</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{path('app_user_scores')}}">Mes scores</a>
						</li>
						<li>
							<a class="dropdown-item" href="{{path('app_scores_index')}}">Wall of fame</a>
						</li>
					</ul>
				</li>
				{% if not app.user %}
					<li class="nav-item">
						<a class="nav-link" href="{{path('app_login')}}">Se connecter</a>
					</li>
					<a class="nav-link" href="{{path('app_register')}}">S'inscrire</a>
					<li class="nav-item"></li>
				</li>
			{% endif %}
		</ul>
		{% if app.user %}
			<div class="text-end text-light">
				<i class="bi bi-person-fill fs-3 align-middle text-primary"></i>
				{{ app.user.userIdentifier }}
				<a class="btn btn-sm btn-outline-warning ms-1" href="{{ path('app_logout') }}" role="button">
					Se déconnecter<i class="bi bi-door-open-fill ms-1"></i>
				</a>
			</div>
		{% endif %}
	</div>
</div></nav>
